---
description: Core principles and philosophy for TinyBigUI development
globs: 
alwaysApply: true
---

# TinyBigUI Core Principles

You are developing TinyBigUI, an open-source Material Design 3 React component library with accessibility-first principles.

## Philosophy

**When in doubt, prioritize: Accessibility > Consistency > Convenience**

## Core Principles

- **Quality over speed**: Build it right, not fast
- **Accessibility first**: WCAG 2.1 AA is non-negotiable
- **MD3 strict adherence**: Follow Material Design 3 specifications exactly
- **TypeScript strict mode**: No `any` types allowed
- **RSC compatible**: Explicit `'use client'` for interactive components

## Tech Stack

- **React**: 18+ with RSC support
- **TypeScript**: Strict mode enabled
- **Tailwind CSS**: v4.x (CSS-first tokens via `@theme`)
- **React Aria**: For accessibility primitives
- **CVA**: For component variant management
- **Vitest + RTL**: For testing
- **Storybook**: For documentation

## Project Structure

```
tinybigui/
├── packages/
│   ├── react/          # @tinybigui/react - Components
│   └── tokens/         # @tinybigui/tokens - Design tokens (CSS)
```

## Task Execution (Learning Mode)

**This is a learning project. The user wants to understand every change.**

1. **ONE TASK AT A TIME**: Complete exactly one task, then STOP
2. **DETAILED EXPLANATION**: After each task, explain what, why, and how
3. **WAIT FOR APPROVAL**: Do NOT proceed until the user explicitly approves
4. **ASK IF UNCLEAR**: If a task has ambiguity, ask before implementing

## Reference Priority

1. Strategy docs (`learning-docs/strategies/` folder)
2. Material Design 3 specs (m3.material.io)
3. React Aria docs (react-spectrum.adobe.com/react-aria)
4. WCAG guidelines (w3.org/WAI/WCAG21)

## MCP Servers (Direct Documentation Access)

This project has MCP servers configured in `.cursor/mcp.json` for direct documentation access:

- **React Aria**: Use for hooks documentation (`useButton`, `useFocusRing`, etc.)
- **Material Design 3**: Use for MD3 component specs, tokens, accessibility guidance
- **Context7**: Use "use context7" for Tailwind CSS v4, CVA, and npm package docs

These servers reduce token consumption and provide always up-to-date documentation.
